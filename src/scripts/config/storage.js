// Session storage key used to remember which section should dezoom on return.
export const RETURN_SECTION_KEY = 'presentation.returnSection';

// Local storage key used to persist appearance settings across pages.
export const THEME_STORAGE_KEY = 'portfolio.theme';
